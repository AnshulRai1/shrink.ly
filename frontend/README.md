# TinyLink Frontend

Vite + React + Tailwind frontend for TinyLink.

## Setup
1. npm install
2. Create .env from .env.example and set VITE_API_URL to your backend API base (e.g. http://localhost:8000/api).
3. npm run dev

## Build
npm run build
